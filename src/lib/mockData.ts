// Los tipos se centralizaron en src/lib/types.ts
// Este archivo se mantiene por compatibilidad de importaciones existentes
export type { BannerSize, BannerMaterial, BannerCondition, InventoryItem } from './types';
